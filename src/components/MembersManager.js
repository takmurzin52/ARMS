export default function MembersManager() {
    return (
        <div>
            <h2 style={{ fontSize: '20px', marginBottom: '16px' }}>Участники</h2>
            <p>Управление участниками команд (будет реализовано позже).</p>
        </div>
    );
}