export default function TeamsManager() {
    return (
        <div>
            <h2 style={{ fontSize: '20px', marginBottom: '16px' }}>Команды</h2>
            <p>Список и управление командами (будет реализовано позже).</p>
        </div>
    );
}